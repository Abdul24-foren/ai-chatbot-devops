apiVersion: v1
kind: Service
metadata:
  name: ai-chatbot-backend
  namespace: ai-chatbot
spec:
  selector:
    app: ai-chatbot-backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
